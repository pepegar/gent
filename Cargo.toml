[package]
name = "gent"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[features]
default = []
embedded = []

[dependencies]
janetrs = { version = "0.8", default-features = true }
ureq = "2"
serde_json = "1"
crossterm = "0.28"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
